rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // 개발 단계: 모든 사용자에게 읽기/쓰기 허용
    // 프로덕션에서는 인증 필요로 변경 필요
    match /{document=**} {
      allow read, write: if true;
    }
  }
}

