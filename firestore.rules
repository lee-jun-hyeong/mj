rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow read/write access to all users (customize based on your needs)
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}

