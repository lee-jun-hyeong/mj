rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // 사용자는 자신의 파일만 업로드/읽기 가능
    match /scores/{userId}/{scoreId}/{allPaths=**} {
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }

    // 기타 파일은 인증된 사용자만 접근
    match /{allPaths=**} {
      allow read, write: if request.auth != null;
    }
  }
}
