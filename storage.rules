rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // 개발 단계: 모든 파일 공개 읽기/쓰기 허용
    // 프로덕션에서는 인증 필요로 변경 필요
    match /{allPaths=**} {
      allow read, write: if true;
    }
  }
}

